% PDF output settings. Doxyfile pins USE_PDFLATEX=YES, so the dvi/ps2pdf
% branch that used to live here has been dropped.
\usepackage[pdftex,
            hyperindex,
            pdffitwindow      = true,
            pdfstartview      = Fit,
            colorlinks        = true,
            linkcolor         = companycolor,
            filecolor         = blue,
            citecolor         = darkgreen,
            bookmarksopen     = true,
            bookmarksopenlevel = 0,
            plainpages        = false,
            pdfpagelabels     = true,
            pagebackref       = true,
            pdftoolbar        = false,
            unicode           = true
           ]{hyperref}

\pdfminorversion=7
